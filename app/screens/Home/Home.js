import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Home = () => {
  return <View style={{ backgroundColor: "cyan", flex: 1 }} />;
};

export default Home;

const styles = StyleSheet.create({});
